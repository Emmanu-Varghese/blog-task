<%= turbo_stream_from :announcement_list %>
<h1>What's New</h1>
<div class="card">
  <div class="card-body">
    <%= turbo_frame_tag "announcement_handler" %>
    <div id="announcements">
      <%= render(AnnouncementComponent.with_collection(@announcements)) %>  
    </div>
  </div>
</div>
<%= render "pager", pagy: @pagy %>